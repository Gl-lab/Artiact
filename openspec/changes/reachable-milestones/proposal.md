# R22 — Reachable intermediate milestones

After R25.1 design (`e4cf63a`), retain the useful unlock as the first evaluation. Only inventory/budget rejection permits one separate evaluation of the next skill level. Preserve the original target and reason; intermediate general-development utility is 0.1, unlock utility zero. No fallback for invalid data/access or already-nearest goals. Needs-mode slices remain R25 work under its parent contract.
