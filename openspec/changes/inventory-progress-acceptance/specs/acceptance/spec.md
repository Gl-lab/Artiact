# Acceptance cases

The anonymized 100/43/57 state with high remaining XP must reject both full and nearest paths with explicit numeric evidence and zero attempts. Separate fixtures must demonstrate intermediate completion with restart, and permitted banking with conserved protected stock. Operator tests must cover Inspect, bounded start, stop/result/reopen and duplicate-click isolation; real browser must show the actual panel's refused Inspect and saved diagnostics. Real API assessment must use one snapshot for six budgets and report zero attempts in every row. Live banking remains excluded.
