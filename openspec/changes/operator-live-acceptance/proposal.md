# R19: Operator live acceptance

Base `a5d5352`. R17/R18 are locally implemented; the historical R16 transient remains open. R19 is not complete until the real operator cycle and its exact evidence exist. The roadmap request and older field-trial approvals do not authorize new gameplay.

First implement local preparation: permit positive small decision budgets (including the protocol's 6, never silently increasing it to 10), and provide a separate read-only autonomous inspection using 2 actions / 6 decisions / 3 no-progress / 120 seconds as planning bounds. It may authenticate only to the pinned official origin, read the named character and required catalogs, and return sanitized fingerprints/candidate/stock facts with zero game POST.

Then capture a fresh character, reviewed revision, exact proposed command allowlist, stock floors and dedicated new RunId/directory. Before any game action, resolve release prerequisites and obtain concrete user authorization for that scope. A guarded live operator host must enforce the approved allowlist at transport, use the real panel and shared lifecycle, and retain unknown state. R20 release/implementation remains gated by successful R19 as required by the roadmap.
