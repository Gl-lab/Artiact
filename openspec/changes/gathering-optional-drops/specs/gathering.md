# Requirements

- Multiple guaranteed and optional entries may be planned if codes are unique, quantities positive/ordered and rates positive integers, with at least one guaranteed entry.
- Every possible simultaneous maximum must fit before scheduling.
- Postconditions require XP/level progress and bounded per-item deltas; undeclared stock and unrelated character fields remain unchanged.
- Invalid tables reject the resource. Rare-only resources are outside this slice.
