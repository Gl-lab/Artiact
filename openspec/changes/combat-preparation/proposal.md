# R4c — Production as combat preparation

Explicit PrepareEquipment policy gates the combat milestone on obtaining/equipping its configured weapon. Reuse R4a shared recipe ledger and add supported loot leaves from the configured normal opponent. All prerequisites share R2 budgets and preserve current combat safety checks. No defeat recovery, consumables or unsupported slots.

Acceptance: two distinct missing loot ingredients are reserved correctly; a synthetic chain fights for materials, crafts, swaps weapon and reaches combat level 3. Unsafe/unsupported source or missing recipe blocks; total budget and lost-command handling remain common.
