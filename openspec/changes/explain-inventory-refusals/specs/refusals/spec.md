# Acceptance

- Inventory refusal reports skill, target, free units, conservative required units, deficit and whether bank policy is configured.
- Budget, unsupported resource and access refusals remain distinguishable, including mixed decisions.
- Refused preparation shows diagnostics without a run or receipt and never starts execution.
- Terminal and archived results expose the same safe diagnostics; old checkpoints show missing details and stale observations remain marked.
- Unknown codes render as text. No raw catalog, delegate, credential or arbitrary path assumption appears in the projection.
- Selection, action authorization, identity and archive gates remain unchanged.
