# R19 authorized field trial — 2026-09-08

Reviewed implementation `96031a3`; invoked HEAD `e18a2aa88a0eea4477ae99eabc29f05f06fa7b22` (publication metadata only after implementation). User explicitly approved the concrete scope with «Даю добро»: gllab, Move:379 then gathering at379 until alchemy2, maximum16 actions/20 decisions/3 consecutive no-progress/900 seconds, copper_ore floor2, R16 historical transient left open for this scoped acceptance.

Invocation: PowerShell try/finally temporarily sets ARTIACT_OPERATOR_LIVE_APPROVAL to the exact documented scope and restores its previous value; `dotnet test Artiact.RealApiTests/Artiact.RealApiTests.csproj --no-build --no-restore --filter Category=RealApiOperatorLive --logger 'console;verbosity=detailed'`. RunId r19-gllab-alchemy2-v1; retained directory `%USERPROFILE%/.artiact-runs/r19-gllab-alchemy2-v1`, outside Git. No second invocation or marker deletion.

Real UI steps: opened http://127.0.0.1:5189/operator in the in-app browser, verified16/900/20/3 fields and correct RunId, clicked Inspect. UI returned `skill:alchemy:sunflower_field`, `Move:379`; clicked Start within receipt freshness. UI confirmed start acceptance. No direct action endpoint was invoked outside the guarded harness.

Outcome: live test passed,13 dispatches/13 decisions (one Move:379 and12 Gather:alchemy),13 Verified journal entries, no pending command, no unknown outcome. Started2026-09-08T13:10:26.7702036Z; finished13:21:54.4430131Z; elapsed687.6728095 seconds (11m27.7s), below900. Test process including preparation lasted13.2984 minutes. Reopen through a fresh shared StagedExecution preserved Cancelled/13 attempts and sent no new POST; transport was sealed first.

Alchemy1/0XP→2/6XP,156 known XP across the level transition. Inventory copper_ore2 unchanged, sunflower+12; other observed skills and HP120 unchanged, final map379. Confirmed cooldown370 seconds (Move10 +12 gathers*30);12 completed waits total340.0819646 seconds. Final30-second cooldown was recorded but its wait was interrupted by the target-level stop. Observed processing346.8920235 seconds is separate. Cancelled is the shared stop-token terminal; the autonomous milestone history records Completed. No checkpoint was archived or cleared.

Offline command: `dotnet Artiact/bin/Debug/net9.0/Artiact.dll run-result <retained-directory> https://api.artifactsmmo.com/gllab`, exit0. Captured output in memory and projected only result/changes/performance; no raw character/catalog payload was committed. Confirmed13 facts, zero candidate switches and no consumed inputs.

UI finding and fix: the field-trial harness shuts down immediately after assertions, so the original panel lost connection while still showing12 actions/143XP. Added an explicitly invoked local-only `Category=OperatorResultView` for two-minute inspection of the retained checkpoint, with controls/executor absent, empty credentials and no API client. A new process served the same real panel; reloaded UI showed stop confirmed,13/16,alchemy2/6XP,370 cooldown seconds and history `alchemy → 2 · Completed`. Generic UI wording says "Остановлен оператором" for this Cancelled terminal even though the stop was requested automatically by the scoped guard. No extra game action was used to obtain this view.

Viewer fix acceptance: no control token/start route and no automatic executor in observation mode. `dotnet test Artiact.RealApiTests/Artiact.RealApiTests.csproj --no-restore --filter FullyQualifiedName~ScopedOperatorHostTests` first failed the new controls=false case (expectedfalse/actualtrue) with four other tests passing. Implemented conditional registration. One compile error for required ApiSettings members was resolved with empty username/password. Final `dotnet test Artiact.RealApiTests/Artiact.RealApiTests.csproj --no-restore --filter Category=RealApiOffline`:74 passed. Local viewer invocation uses ARTIACT_OPERATOR_RESULT_VIEW=1 with try/finally restoration and `dotnet test Artiact.RealApiTests/Artiact.RealApiTests.csproj --no-build --no-restore --filter Category=OperatorResultView`:1 passed after its two-minute window; host stopped and browser tab closed. Final `dotnet test Artiact.sln --no-restore`:576 application +288 mock passed. No final build/test warnings. `git diff --check` passed with routine LF-to-CRLF notices. Self-review covered the viewer's absent executor/controls/credentials, unchanged live-host default, exact outcome arithmetic, distinction between same-process execution reopen and restarted observation process, and retained cancelled state. No independent review claimed.

Scope of acceptance: this named noncombat route and retained terminal state only. The manual Stop-during-POST case remains covered by existing local R18 tests; this live run used automatic target-level stop. There was no second gameplay-host invocation, forced crash, response-loss injection, bank/combat or repeated trial. The separately restarted process only viewed retained state; shared execution reopen was exercised inside the original harness. R16 root cause remains unproven under the explicitly approved scoped exception.
