# Scoped dispatch

- Reject other character/path/body, noninitial first snapshot, altered world, stale or missing observation, outside map, insufficient stock and alchemy>=2 before POST.
- Each dispatch needs new full catalogs/character observation; serialize concurrent calls and never retry failed dispatch.
- Only a one-property map_id379 movement body or bodyless gathering is allowed. Preserve game response for production validation.
- Stop after observing alchemy2 in a reply. Malformed/failed/uncertain replies prohibit further dispatch and retain production reconciliation requirements.
- Local operator host never starts execution automatically. Existing run state cannot authorize a new field trial.
