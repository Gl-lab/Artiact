# Bounded acceptance

Only bodyless gathering for gllab at map 277 is allowed. Production session limits are four attempts, twenty decisions, three no-progress steps and 600 seconds. Two verified gathers span separate checkpoint-store/session instances. Cooperative cancellation persists; reopening stopped state does not dispatch. Existing checkpoints are never automatically removed or reset by this command.
