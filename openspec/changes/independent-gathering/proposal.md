# R1 — Independent gathering portfolio

Deliver a profession-only Inspect/OneShot profile without combat configuration or combat normalization. Existing mixed profiles retain their combat safety boundary. Scope: application strategy/operation and socket-free tests; no live actions, bank, durable runner or cache refresh.

Acceptance: mining alone selects and verifies Move/Gather, even with non-fire combat stats; Inspect dispatches zero actions. Missing profession fields, invalid inventory/access, stale reads and contract drift fail closed. Only maps/resources and relevant API operations are required for a profession-only profile.
