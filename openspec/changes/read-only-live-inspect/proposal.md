# Read-only live Inspect

Roadmap section 9 needs a concrete production-planner decision for the newly created character. Extend the isolated API verification project with explicit opt-in inspection of mining target 2, using the production compatibility probe, observer and strategy session without hosting or action dispatch.

Acceptance: transport rejects every action POST before network I/O; only the configured character, OpenAPI and paginated maps/resources can be read; credentials remain confined to the official origin. The report includes sanitized decision/candidate/command and zero attempts. Existing smoke remains separate. No OneShot, combat, automatic creation or container deployment is part of this slice.
