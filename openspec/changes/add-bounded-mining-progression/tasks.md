## 1. Dependency and baseline evidence

- [x] 1.1 Record exact HEAD/status and existing `.serena/project.yml` modification; run `dotnet build Artiact.sln --no-restore` and `dotnet test Artiact.sln --no-restore`, recording commands, counts and existing warnings before edits.
- [x] 1.2 Verify completed predecessor code/specs against base `098494d`, synchronize their capabilities into the base specs before this change's deltas, and validate the resulting requirement names; do not mark unrelated work archived or implemented without its own evidence.

## 2. Slice one: deterministic destination resolution

- [x] 2.1 Add focused `MiningDestinationResolverTests` for skill casing, exact resource/map join, empty and malformed lists, duplicates, missing maps, locked/zero-XP resources, level-zero compatibility, all tie-breakers, permutation stability and extreme-coordinate arithmetic. Record behavioral RED against current selection where reproducible; compile-only setup is not RED.
- [x] 2.2 Implement the application-local immutable destination, pure ranking and narrow catalog resolver; run the focused tests GREEN, including fallback from an unmapped higher resource and no mutation of input lists. Contrast a provider returning null (typed invalid_mining_catalog) with a provider throwing (propagated loading failure); preserve client parsing/retries.
- [x] 2.3 Add factory tests and implement six progression-only Blocked reasons with exact reason codes/null-fact invariants; run factory and unchanged pure GoalService tests GREEN. Document coordinate-only eligibility and deferred layer/access support in the domain guide and known limitations.

## 3. Slice two: bounded mining step and cooldown seam

- [x] 3.1 Add compiling behavior tests counting at most one invocation each of IGameClient.Move/IGameClient.Gathering (not HTTP attempts or server effects), no redundant move, resource reselection across cycles, target/free-space boundaries before movement and after move, changed level, invalid XP, wrong destination and cancellation after successful responses. Record actual behavioral failures before replacing mining execution; retain craft/loot and generic ActionStep tests.
- [x] 3.2 Add the application-local resolved GatheringGoal subtype, focused mining step and injected cooldown wait; wire StepBuilder to consume a resolved destination once and route plain GatheringGoal callers through the same resolver. Verify focused mining tests GREEN and no changes to Artiact.Contracts or generic loot-repeat behavior.
- [x] 3.3 Verify save-before-cancel and exact returned total cooldown requests through controlled delay tests; run MiningBoundaryTests, StepCancellationTests, StepBuilderTests and LootingCraftPlanningTests. Update the affected architecture/domain flow and test-map documentation with one-gather cycle semantics.

## 4. Slice three: final decisions and run bounds

- [x] 4.1 Add focused `MiningProgressionTests` for exact guard precedence, no-progress/reset/level-up accounting, move-only cycles, invalid returned progress, completion on last allowed attempt, thrown catalog/action attempts consuming budget, independent scopes and successful/failed reinitialization. Record behavioral RED before implementing counter/decision behavior.
- [x] 4.2 Implement scoped run state, ActionService finalization and post-response accounting; share the run state with mining execution, log only the final decision, and retain worker terminal/recovery branching. Verify the focused tests GREEN and no exception-to-success conversions. Characterize real-client cold initialization with resources data:null: loading fails before cycles or mutations; during-cycle loading exceptions consume the reserved attempt and produce no fabricated final decision event.
- [x] 4.3 Add `MiningProgressionConfigurationTests` using the production registration for absent/malformed/zero/negative/inverted values and valid 100/3 values; implement AddMiningProgression, tracked settings and Program DI. Verify startup validation without main-host startup and all changed constructor mocks/callers compile.
- [x] 4.4 Update ActionService and DecisionObservabilityTests for exact final decision identity, destination/counter fields, omitted terminal fields, one event, and listener equivalence. Run worker/orchestration/configuration tests GREEN; update README, architecture, development and known limitations for terminal reasons, counters, reset behavior and settings.

## 5. Slice four: additional deterministic scenario

- [x] 5.1 Add independent literal progression fixture/reset/catalog oracles and scenario-switch tests, plus fixture validation failures; record unknown-scenario RED before adding the second named scenario. Verify basic-mining fixture hashes remain unchanged.
- [x] 5.2 Add MiningProgressionScenario.json and exact-name loading/selection under the existing store boundary; preserve original BasicMiningScenario.json and exact basic envelopes. Run both scenarios' reset/catalog/character tests GREEN.
- [x] 5.3 Add `MiningProgressionScenarioTests` for repeat moves/gathers, exact 1/6 -> 2/2 -> 2/8 -> 3/4 progression, threshold/carry-over/multiple-level awards, output-slot order, complete literal ActionResponse comparisons (character, cooldown, details and destination), iron-ore details across level-up, moves to Iron Rocks/Origin, insufficient level, capacity/slot failures, overflow and atomic rejection. Implement only the needed transitions after recording behavioral RED; rerun focused tests GREEN.
- [x] 5.4 Extend replay/concurrency tests to progression, comparing all normalized fields and complete before/after snapshots, and run the existing boundary/route/basic compatibility suite GREEN. Update docs/mock-service.md and MockService AGENTS.md to name both fixtures, scenario rules and explicit synthetic XP divergence.

## 6. Slice five: full-client progression acceptance

- [x] 6.1 Add `MiningProgressionFlowTests` using real application services and real GameClient/GameHttpClient through the existing TestServer-only factory, in-memory cache and recorded instant waits. Assert the exact five-cycle/six-action/34-second oracle in design.md, four Selected events then Completed, final inventory order, every successful action response against independent full-field expected values, cold startup, no extra action and no sockets/cache/credentials/main-host access. If already GREEN, record that without manufacturing RED.
- [x] 6.2 Run the full sequence twice with explicit reset and fresh initialized scopes; compare every state/trace/action-response field after normalizing generation only; retain independent literal response oracles as well as replay equality. Verify both manual bounded-cycle driver and worker terminal flow, with no real cooldown sleep.
- [x] 6.3 Cover initial/exhausted inventory, initial target reached, MaxCycles 1, unchanged XP, wrong movement result, invalid snapshots and cancellation with controlled fixtures at the appropriate service boundary; assert exact action counts/reasons and that terminal results add no recovery delay. Do not add mock HTTP control endpoints just for failure injection.
- [x] 6.4 Update the development test map/commands and README completion claim with the named scenario and remaining limits; verify affected diagrams, nested instructions and prose match the final code without duplicating changing test totals in guides.

## 7. Final verification and review evidence

- [x] 7.1 Run `dotnet test Artiact.Tests/Artiact.Tests.csproj --no-restore --filter "FullyQualifiedName~Mining|FullyQualifiedName~GoalDecision|FullyQualifiedName~ActionService|FullyQualifiedName~DecisionObservability|FullyQualifiedName~StepCancellation|FullyQualifiedName~StepBuilder|FullyQualifiedName~LootingCraftPlanning|FullyQualifiedName~ArtiactBackgroundService"` and `dotnet test Artiact.MockService.Tests/Artiact.MockService.Tests.csproj --no-restore`; record exact results and any uncovered acceptance cases.
- [x] 7.2 Run `dotnet build Artiact.sln --no-restore` and `dotnet test Artiact.sln --no-restore` on the final diff; distinguish existing warnings from regressions. Run separately restored RealApiOffline only if shared API DTOs or its boundary code unexpectedly changed; never run live tests or the main host for this slice.
- [x] 7.3 Run `npx -y @fission-ai/openspec@1.12.0 validate add-bounded-mining-progression --strict` and `git diff --check`; inspect final status/diff for cache, secrets, generated artifacts and unrelated changes, preserving `.serena/project.yml`.
- [x] 7.4 Review acceptance, overlapping predecessor deltas, decision/counter/cancellation boundaries, deterministic oracles and unrelated complexity against the exact final diff. Record review type and revision/diff identity; reproduce blockers before fixes, rerun affected gates after fixes, and list unverified scope. Do not claim independent review when only self-review occurred.

Implementation acceptance and offline verification are complete. The final evidence below supersedes earlier checkpoint status and records test-first process deviations explicitly. No predecessor is archived by this change.

## Planning verification evidence (2026-09-06)

- Reviewed base: `098494da5cf4a5ff9a7af451c4268abc9068cce8`; candidate is the new `openspec/changes/add-bounded-mining-progression/` directory only. Review type: self-review of source alignment, acceptance arithmetic, full predecessor requirement blocks, terminal precedence, scope and task coverage. No independent review is claimed.
- `npx -y @fission-ai/openspec@1.12.0 validate add-bounded-mining-progression --strict`: passed. Two informational archive-preflight messages remain: `deterministic-mock-scenario` and `explainable-mining-goal-selection` do not yet exist in base specs. Their completed predecessor changes must be synchronized first as recorded in task 1.2. This change is a complete proposal, not archive-ready implementation.
- `npx -y @fission-ai/openspec@1.12.0 status --change add-bounded-mining-progression`: all four planning artifacts complete; all implementation tasks remain unchecked.
- `dotnet test Artiact.sln --no-restore`: passed, 221 application tests and 46 mock tests, 267 total; zero failed/skipped. This verifies the unchanged code baseline, not the proposed new behavior. Existing compiler/nullability/analyzer warnings and NU1902 for OpenTelemetry.Exporter.Zipkin 1.12.0 were reported.
- `git diff --check`: passed for tracked changes. New planning files were additionally checked individually with `git diff --no-index --check -- /dev/null <file>` because they are untracked.
- CLI invocations reported the environment's existing `NODE_TLS_REJECT_UNAUTHORIZED=0` warning; this task did not set or change that variable.
- Unverified: future implementation, live API/OpenAPI payload compatibility, real-world map access, main-host execution and production rollout. No production source, tests, configuration, cache or predecessor artifacts were edited; `.serena/project.yml` remains an unrelated local change. No commit or push was performed.

### Review corrections applied (2026-09-06)

The user authorized all three P2 specification corrections from the review of planning manifest `46E5E379EA7EB3D95DA61A9742522B4A49A531CABE6213F6A3A5A702568768AE` at base `098494d`:

- Action limits now explicitly count application-to-client method invocations. Internal POST retries and exactly-once server effects remain outside the guarantee; successful mock trace counts assume retry-free responses.
- Invalid catalog values returned to the resolver are distinguished from client loading/parsing exceptions. Added cold initialization with resources data:null and returned-null versus thrown-provider scenarios, with matching implementation tasks.
- Progression move/gather responses now define details, destination, complete committed character and cooldown. Added independent full-response assertions, including iron details across level-up and movement to Iron Rocks/Origin.

Self-review checked these changes across proposal, design, requirements and tasks. Repeated `npx -y @fission-ai/openspec@1.12.0 validate add-bounded-mining-progression --strict` passed with the same two documented predecessor-sync informational messages; status remained 4/4 planning artifacts. Repeated `dotnet test Artiact.sln --no-restore` passed 221 application plus 46 mock tests, zero failed/skipped, with existing NU1902 warning. Tracked `git diff --check` and individual new-file whitespace checks passed. These are document corrections and unchanged-code baseline checks; future implementation and live behavior remain unverified.

## Implementation evidence: baseline and slice one (2026-09-06)

Completed tasks 1.1–2.3 only. Next task is 3.1. The resolver and progression reason factory are independently usable and tested, but ActionService/StepBuilder integration, bounded execution, run counters, configuration, the second mock scenario and full-client acceptance remain unimplemented. No whole-change completion or archive is claimed.

- Base HEAD: `098494da5cf4a5ff9a7af451c4268abc9068cce8`. Initial status: modified `.serena/project.yml` and untracked `openspec/changes/add-bounded-mining-progression/`. No commit, staging or push performed.
- `.serena/project.yml` SHA-256 before/after: `55EFEC5F37D514A138669A1573852ABA1FE90E6EE023C3601943C91B8A905898`; preserved unchanged.
- Baseline `dotnet build Artiact.sln --no-restore`: passed, zero errors, one NU1902 warning. Baseline `dotnet test Artiact.sln --no-restore`: passed 221 application + 46 mock tests, zero failed/skipped.
- Predecessor synchronization: copied complete added requirement blocks for `explainable-mining-goal-selection` and `deterministic-mock-scenario` into base specs, and replaced only the two matching orchestration requirement blocks from the explainable predecessor. Other requirements are preserved. Their completed task records, baseline source/tests and current passing solution support this synchronization. Neither predecessor was archived or otherwise reclassified. The new change's deltas remain in its change directory until implementation is complete.
- Behavioral RED: before production edits, `dotnet test Artiact.Tests/Artiact.Tests.csproj --no-restore --filter FullyQualifiedName~MiningDestinationResolverTests` compiled and failed its single `MixedCaseMiningCanSelectMappedResource` test with `NullReferenceException` at existing `StepBuilder.cs:63` for resource skill `MiNiNg`. The final test exercises the new independent resolver. This demonstrates the original defect and resolver correction, not a correction to the still-unmigrated StepBuilder. The broader resolver/factory tests were first executed after their implementation and were GREEN; no separate test-first RED is claimed for them.
- Focused `dotnet test Artiact.Tests/Artiact.Tests.csproj --no-restore --filter "FullyQualifiedName~MiningDestinationResolverTests|FullyQualifiedName~GoalDecisionFactoryTests|FullyQualifiedName~GoalServiceTests"`: first run 121 passed / 1 failed because the old factory reflection test asserted exactly six reasons. Updated that assertion to twelve for the explicit enum extension; final run passed 122, zero failed/skipped. Pure GoalService behavior and tests are unchanged.
- Acceptance coverage: skill casing; exact code/type joins; empty/malformed/null catalogs; duplicates; ignored empty map content; locked/non-mining/zero-XP candidates; level-zero compatibility; unmapped high-resource fallback; level/distance/ordinal-code/X/Y ordering; all 36 permutations of a three-resource/three-map fixture; input and result isolation; extreme coordinate and level arithmetic; returned null versus unchanged thrown resource/map loading exception; all six new reason codes and invalid/null-fact factory shapes.
- Final `dotnet build Artiact.sln --no-restore`: passed, zero errors, one NU1902 warning. Final `dotnet test Artiact.sln --no-restore`: passed 262 application + 46 mock tests (308 total), zero failed/skipped. Compilation of changed source also surfaced existing CS8600/CS8602/CS0162/CS1998 in unchanged StepBuilder and ASP0000 in unchanged Program; no diagnostics referred to new source.
- `npx -y @fission-ai/openspec@1.12.0 validate add-bounded-mining-progression --strict`: passed; the two missing-predecessor informational messages are gone. The environment still reports its pre-existing `NODE_TLS_REJECT_UNAUTHORIZED=0` warning; this task did not alter it.
- `git diff --check`: passed. All untracked files were additionally checked with `git diff --no-index --check -- /dev/null <path>`; no whitespace failures. Final status contains only the preserved Serena edit, original planning files, source/tests/docs for this slice and synchronized base specs. No Contracts, client transport, configuration, mock fixture, tracked cache or generated-file changes.
- Self-review of source, acceptance tests, full tracked diff and new files: no slice-one blockers found; no independent review claimed. Candidate manifest SHA-256: `9D70A202604E2488F33566A104F32CB074CA7479E593519245A201A509222FEA`. Identity is SHA-256 of UTF-8 without BOM of sorted unique changed/untracked paths, each line `<path> <uppercase file SHA-256>`, joined by LF without final LF; excludes `.serena/project.yml`, `*/tasks.md` and `*/implementation-evidence.md` so this evidence does not recursively alter its own identity.
- Documentation: domain guide, development test map and known limitations describe the new independently tested resolver and explicitly pending execution integration. Existing runtime diagrams still match unchanged orchestration. Root/application/test instructions remain applicable without edits.
- Unverified: tasks 3–7 and runtime integration; live API compatibility, layers/access and production behavior. The main host and live tests were not run. RealApiOffline was not required because shared DTOs and its boundary code did not change. Review and gates here apply to this slice, not the final gates in section 7.

## Final implementation and verification evidence (2026-09-06)

The bounded mining capability, named progression scenario and offline acceptance are implemented. Tasks 3–7 are complete for behavior/verification; test-first process deviations below are recorded rather than retroactively inventing RED evidence. Earlier checkpoint statements about pending integration or no publication are historical. User explicitly authorized completing the change with commit and push.

### Behavior and acceptance map

- MiningStep/ResolvedMiningGoal replace mining's generic repeat graph. The same run state is shared by ActionService and StepBuilder through production scoped registration. Plain GatheringGoal resolves through the same catalog algorithm; resolved goals do not reselect. Generic ActionStep, craft and bounded loot behavior are unchanged.
- MiningExecutionBoundaryTests and MiningStepTests cover invocation limits, live pre-move/after-move guards, changed level, wrong coordinates, no redundant movement, save/account-before-cancel, controlled cooldown cancellation and exact total seconds. The existing MiningBoundaryTests retain malformed inventory/level and two-cycle terminal worker assertions with the new catalog setup and one-gather semantics.
- MiningProgressionTests and MiningRunTests cover invalid XP, guard precedence, unchanged/decreased progress, XP reset/level-up, movement-only cycles, completion on the last permitted attempt, loading/action exceptions consuming budget, invalid responses and successful/failed/cancelled reinitialization. MiningProgressionConfigurationTests exercise production binding/startup validation and independent scopes. ActionServiceTests and DecisionObservabilityTests verify final identity, fresh resolved goals, exact fields, event cardinality and optional-listener equivalence; new progression-terminal telemetry is covered in MiningRunTests.
- MiningProgressionScenarioTests compare independent full catalog/character/action literals, ordered slots/items, exact XP carry/threshold/multiple awards, iron details across level-up, movement to Iron Rocks and Origin, basic/progression switching, immutable observations, malformed definitions, capacity/slot/level/XP/time rejection and complete atomic before/after snapshots. Replay and concurrent gather/read/reset/catalog cases retain normalized generation as the only ignored state field.
- MiningProgressionFlowTests construct real services and real GameClient/GameHttpClient through the existing TestServer factory, in-memory cache, sentinel credentials and recorded instant waits. Both manual and worker drivers perform two reset/fresh-scope runs: four Selected then Completed, six actions, waits [7,5,5,7,5,5], virtual elapsed 34 seconds, final level/XP/max XP 3/4/10 with two copper and two iron in ordered slots. Every successful response matches an independent complete DTO oracle. Replay additionally compares all state, trace, response, decision/event and wait fields with generation normalized only. Initial/exhausted inventory, initial target, budget one, unchanged XP, wrong movement, invalid snapshots and controlled cancellation are covered without new mock control endpoints or terminal recovery delays.
- Real-client resources data:null is explicitly tested during cold initialization (zero attempts, no character read/actions/decision event) and during a cycle after cache clearing (one consumed attempt, original loading exception, no fabricated final event). No client parsing or retry changes were made.
- ResolvedMiningGoal decomposition never introduces legacy inventory remediation if live inventory changes after planning. Independently supplied plain goals retain their existing decomposition semantics.

### Observed RED/GREEN and process deviations

- Before replacing mining execution, `dotnet test Artiact.Tests/Artiact.Tests.csproj --no-restore --filter FullyQualifiedName~MiningStepTests` failed behaviorally: expected one gather, observed two. The new bounded step passes it.
- Before progression finalization, `dotnet test Artiact.Tests/Artiact.Tests.csproj --no-restore --filter FullyQualifiedName~MiningProgressionTests` failed behaviorally: expected InvalidMiningProgress, observed MiningBelowTarget for XP -1. It passes after guard integration.
- Before adding the second named scenario, `dotnet test Artiact.MockService.Tests/Artiact.MockService.Tests.csproj --no-restore --filter FullyQualifiedName~MiningProgressionScenarioTests` failed: reset expected HTTP 200, observed 404. The implemented scenario passes.
- Self-review reproduced an unintended legacy FindTargets call for a resolved mining goal with changed inventory using `dotnet test Artiact.Tests/Artiact.Tests.csproj --no-restore --filter FullyQualifiedName~ResolvedMiningNeverIntroducesInventoryRemediation`; it failed on that unexpected call before the specific decomposition guard and passes after it.
- The remaining detailed transition/counter/configuration tests were first executed after the corresponding implementation and were GREEN. Therefore the planned strict test-first ordering was not followed for every subcase of tasks 3.1, 4.1, 4.3 and 5.3. This is a process deviation, not evidence of pre-implementation RED. Coverage and final gates are complete; no missing-type compilation failure or artificial implementation rollback was counted as behavioral RED.
- First full-client execution exposed a harness setup error: reset had started requests on the HttpClient before GameHttpClient assigned BaseAddress. Reset now uses a separate HttpClient over the same TestServer handler. After that harness correction all eight initial flow cases were GREEN without a production correction; the expanded suite passes 13 cases. This setup failure is not product RED evidence.

### Exact final gates

Base revision remains `098494da5cf4a5ff9a7af451c4268abc9068cce8`. Review type: self-review of changed source, tests, documentation, active deltas, synchronized predecessor requirements and complete candidate file set. No independent review is claimed. No reproduced blocker remains open. Candidate manifest SHA-256 (same path/file-hash algorithm and evidence exclusions as the slice-one record): `EE9AE1F8C725F7881359EE30CE85E082BE7DF95707B9F03F9ACEEE024E38310C`.

| Command | Result |
|---|---|
| `dotnet test Artiact.Tests/Artiact.Tests.csproj --no-restore --filter "FullyQualifiedName~Mining|FullyQualifiedName~GoalDecision|FullyQualifiedName~ActionService|FullyQualifiedName~DecisionObservability|FullyQualifiedName~StepCancellation|FullyQualifiedName~StepBuilder|FullyQualifiedName~LootingCraftPlanning|FullyQualifiedName~ArtiactBackgroundService"` | Passed 262, zero failed/skipped |
| `dotnet test Artiact.MockService.Tests/Artiact.MockService.Tests.csproj --no-restore` | Passed 80, zero failed/skipped |
| `dotnet build Artiact.sln --no-restore` | Passed, zero errors; existing NU1902 warning |
| `dotnet test Artiact.sln --no-restore` | Passed 317 application + 80 mock = 397, zero failed/skipped |
| `npx -y @fission-ai/openspec@1.12.0 validate add-bounded-mining-progression --strict` | Passed |
| `npx -y @fission-ai/openspec@1.12.0 validate --all --strict` | Passed all seven items; informational long-requirement notices only |
| `git diff --check` and individual `git diff --no-index --check -- /dev/null <new-file>` | Passed after removing two introduced trailing spaces |
| `git diff --exit-code HEAD -- Artiact/cache Artiact.Contracts Artiact.MockService/BasicMiningScenario.json` | Passed; unchanged |
| `git fetch origin` then `git rev-list --left-right --count HEAD...origin/master` | Successful; 0/0 before publication |

Changed-source compilation still reports existing StepBuilder CS0162/CS1998/CS8602 and Program ASP0000 diagnostics. No new-code warning remains. NU1902 concerns the unchanged OpenTelemetry.Exporter.Zipkin 1.12.0 dependency. OpenSpec also reports the environment's pre-existing NODE_TLS_REJECT_UNAUTHORIZED=0 warning; no environment change was made.

BasicMiningScenario.json SHA-256 is `C2DBD62E7BBF92A564AAC839813D69AC7C3B008E006CFECBE5AB657E0883FBD0`; Git comparison confirms byte preservation. The unrelated unstaged .serena/project.yml remains `55EFEC5F37D514A138669A1573852ABA1FE90E6EE023C3601943C91B8A905898` and is excluded from publication. No secrets, tracked cache refreshes, generated build artifacts, package changes, HTTP retry changes or shared API DTO changes enter this change.

Updated docs: README, architecture and action diagram, domain model, development test map/commands/settings, known limitations, mock-service guide and MockService AGENTS.md. No remaining runtime prose claims pending integration or basic-only support.

Unverified by design: live API/OpenAPI compatibility, real-world layers/access, exactly-once server effects, main-host startup and production rollout. RealApiOffline is not required because shared DTOs and its boundary code are unchanged. No live tests or main application host were run. The current change and predecessors remain available as OpenSpec change artifacts; no unrelated archival was performed. Publication outcome is recorded in the final handoff with the resulting commit.
