# R17: Local observation panel

Base: `47d7f5f`. The user approved local R17/R18 while the historical R16 transient remains open. Release acceptance continues to carry that limitation.

Provide a local, opt-in `/operator` panel and `/operator/snapshot` read API. Show worker activity separately from host liveness, durable run identity, goal/origin, confirmed action, resource/action/time budgets, observed character facts, freshness, terminal reason, milestone history and archived run summaries. No game calls on refresh. No new frontend dependencies. Projects: Artiact and tests; no shared Contracts or API changes.
