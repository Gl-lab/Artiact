# R10 execution evidence — 2026-09-07

Base `394a707` on `codex/autonomous-roadmap`; reviewed final R10 diff and new files before staging. Self-review covered context keys, durable facts before wait, no learning on lost replies, budget/feasibility preservation, public formulas as assumptions, actual item/bank attribution, candidate alternatives and configuration compatibility. No independent agent review. User `.serena/project.yml` is excluded.

RED evidence before implementation: `dotnet test Artiact.Tests/Artiact.Tests.csproj --no-restore --filter FullyQualifiedName~FullPathSelectionTests` failed the changed-equipment sample count (1 instead of 0) and selected the expensive-preparation candidate a instead of b. Public record scaffolding existed but no selection change when these assertions ran. The initial context test used Inspect, which does not restore checkpoints, and was corrected to Tick before claiming RED. Both assertions then failed and became green with context/full-cost implementation.

Review found additional observable alternatives defects: two unsupported opponents reused the same candidate ID, and consumable wrapping collapsed the combat alternatives before full-cost comparison. The focused existing autonomous-comparison test was extended to two rejected opponents and failed selection; unique rejection IDs fixed it. The full-path food scenario gained a second gear alternative and failed the two-route assertion; preserving every feasible wrapped parent and its route fixed it. This exposed a rejected-route sentinel cost exceeding candidate bounds and invalidating a valid alternative; the rejected route now has no executable cost rather than an overflowing sentinel.

Tests added with implementation, not claimed as prior RED: actual XP/inputs, exact one-level versus unknown multi-level progress, bank-transfer conservation, interrupted wait, zero legacy fact coverage, XP-sensitive selection and deterministic comparisons. [Comparisons](comparisons.md) record wins/ties/losses and complete real-client oracles. No fabricated duration/progress sample is added for reconciliation.

Verification commands: `dotnet build Artiact.sln --no-restore`; `dotnet test Artiact.sln --no-restore`; `dotnet test Artiact.RealApiTests/Artiact.RealApiTests.csproj --no-restore --filter Category=RealApiOffline`. Final results after the alternatives fixes: 524 application, 216 mock/process and 54 offline boundary tests passed, no failures/skips; build zero warnings/errors. Focused ActionFacts/FullPathSelection/RunLifecycle tests and FullPathMode/Autonomous/HealingSupply checks also passed. `git diff --check` passed.

Public skill/combat/equipment references were read without credentials. No main app/game action, live comparison, simulator calibration or deployment was performed. Estimates retain explicit unknown training/future-level/travel/yield assumptions. R10 does not promise global optimum or live gain. All code-level roadmap epics are locally delivered; the R6 live mining/bank protocol and live-combat decision remain separate.
