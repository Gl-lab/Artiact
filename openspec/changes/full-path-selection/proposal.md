# R10: measured full-path selection

Extend opt-in R5 selection with observed XP/item progress and route costs while preserving its default behavior. Expose verified action facts, separate cooldown from elapsed wall time, retain preparation/material/food attribution, and isolate measurements by relevant state/catalog context. Compare bounded alternatives and report wins, ties and losses against fixed policies without claiming global optimality.

Affected: Strategy candidates/session/measurement, finite production estimation, portfolio settings, durable journal/result output, tests and docs. No live actions or shared DTO change.
