# Logging noise reduction — 2026-09-08

Base: `16276df`, self-reviewed local diff in application logging/configuration, logging tests, process recovery test completion marker, architecture and development documentation. The unrelated `.serena/project.yml` modification is excluded.

Acceptance: routine ASP.NET/HttpClient Information events and console trace dumps are quiet by default; application/host lifecycle Information plus warnings/errors remain. Character reads and Selected goal decisions use Debug; terminal goal decision fields and activity tags are preserved. Staged results use one compact Information summary, with full JSON generated only for Debug. NLog no longer duplicates system console events and writes application Info+ to its file. `Telemetry:ConsoleExporterEnabled` is opt-in; OTLP and Prometheus configuration remains. Deployment logging overrides can still change defaults.

Verification:

- `dotnet test Artiact.Tests/Artiact.Tests.csproj --no-restore --filter "FullyQualifiedName~LoggingDefaultsTests|FullyQualifiedName~DecisionObservabilityTests"`: initial behavioral RED: 8 failures (HTTP Information still enabled and Selected decisions still Information); final 44 passed.
- First `dotnet test Artiact.sln --no-restore` exposed process recovery timeouts waiting for the former `Staged decision` console marker. The run was interrupted after diagnosis. Updated only its completion marker to `Staged result:`; retained all durable journal, action count, replay and recovered outcome assertions.
- Final `dotnet test Artiact.sln --no-restore`: 615 application + 301 mock/compatibility tests passed, no failures or skips, including all real child-process recovery cases with the reduced logging defaults.
- `git diff --check`: passed.

No production host or game actions were started. Tests exercised mock-only child hosts and preserved the failure/recovery oracles. The other computer's running configuration, console volume, NLog file contents and live OTLP receiver were not inspected. Documentation includes console Debug/HTTP overrides and the optional trace exporter setting.
